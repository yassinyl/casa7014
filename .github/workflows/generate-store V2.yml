name: Migrate Casa7014 to V2

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: casa7014-v2
  cancel-in-progress: false

jobs:
  migrate-v2:
    runs-on: ubuntu-latest

    steps:

      # ==========================================================
      # CHECKOUT
      # ==========================================================

      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: main

      # ==========================================================
      # DEPENDENCIES
      # ==========================================================

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3 \
            python3-yaml \
            jq \
            docker-compose-plugin

      # ==========================================================
      # MIGRATE ALL APPS
      # ==========================================================

      - name: Migrate Apps to V2
        shell: bash
        run: |
          set -euo pipefail

          python3 <<'PY'
          import os
          import re
          import yaml

          APPS_DIR = "Apps"

          ALLOWED_CATEGORIES = {
              "Media",
              "Productivity",
              "Home",
              "Networking",
              "AI",
              "Finance",
              "Social",
              "Developer",
              "Others",
          }

          CATEGORY_MAP = {
              "Casa7014": "Others",
              "Other": "Others",
              "Others": "Others",

              "Utility": "Productivity",
              "Utilities": "Productivity",
              "Tools": "Productivity",
              "Tool": "Productivity",

              "Network": "Networking",
              "Networking": "Networking",

              "Development": "Developer",
              "Developer": "Developer",

              "Media": "Media",
              "Productivity": "Productivity",
              "Home": "Home",
              "AI": "AI",
              "Finance": "Finance",
              "Social": "Social",
          }

          def normalize_locales(value):

              if isinstance(value, dict):

                  result = {}

                  for key, val in value.items():

                      if key.lower() == "en_us":
                          key = "en_US"

                      elif key.lower() == "zh_cn":
                          key = "zh_CN"

                      result[key] = normalize_locales(val)

                  return result

              if isinstance(value, list):

                  return [
                      normalize_locales(v)
                      for v in value
                  ]

              return value

          def make_id(app_name):

              slug = re.sub(
                  r"[^a-zA-Z0-9]+",
                  "-",
                  app_name
              ).strip("-").lower()

              return f"com.casa7014.{slug}"

          def get_existing_port(xc):

              value = xc.get("port_map")

              if value is None:
                  return None

              value = str(value).strip()

              if value.isdigit():
                  return value

              return None

          def get_compose_port(data):

              services = data.get(
                  "services",
                  {}
              )

              for service in services.values():

                  ports = service.get(
                      "ports",
                      []
                  )

                  if not isinstance(
                      ports,
                      list
                  ):
                      continue

                  for port in ports:

                      if isinstance(
                          port,
                          int
                      ):
                          return str(port)

                      if not isinstance(
                          port,
                          str
                      ):
                          continue

                      value = port.strip()

                      if "/udp" in value.lower():
                          continue

                      value = value.replace(
                          "/tcp",
                          ""
                      )

                      parts = value.split(":")

                      # host:container
                      if len(parts) >= 2:

                          published = parts[-2]

                          if published.isdigit():
                              return published

                      # single port
                      elif len(parts) == 1:

                          if parts[0].isdigit():
                              return parts[0]

              return None

          def get_environment_port(data):

              services = data.get(
                  "services",
                  {}
              )

              preferred = [
                  "HTTP_PORT",
                  "WEB_PORT",
                  "SERVER_PORT",
                  "PORT",
                  "METRICS_PORT",
                  "CSS_METRICS_PORT",
              ]

              candidates = []

              for service in services.values():

                  env = service.get(
                      "environment",
                      []
                  )

                  if isinstance(
                      env,
                      dict
                  ):

                      items = env.items()

                  elif isinstance(
                      env,
                      list
                  ):

                      items = []

                      for item in env:

                          if not isinstance(
                              item,
                              str
                          ):
                              continue

                          if "=" not in item:
                              continue

                          key, value = item.split(
                              "=",
                              1
                          )

                          items.append(
                              (
                                  key,
                                  value
                              )
                          )

                  else:

                      continue

                  for key, value in items:

                      if not isinstance(
                          value,
                          str
                      ):
                          continue

                      value = value.strip()

                      if not value.isdigit():
                          continue

                      if key.upper().endswith(
                          "_PORT"
                      ):

                          candidates.append(
                              (
                                  key.upper(),
                                  value
                              )
                          )

              for wanted in preferred:

                  for key, value in candidates:

                      if key == wanted:
                          return value

              if candidates:
                  return candidates[0][1]

              return None

          errors = []

          for app_name in sorted(
              os.listdir(APPS_DIR)
          ):

              app_dir = os.path.join(
                  APPS_DIR,
                  app_name
              )

              if not os.path.isdir(
                  app_dir
              ):
                  continue

              compose_path = os.path.join(
                  app_dir,
                  "docker-compose.yml"
              )

              if not os.path.isfile(
                  compose_path
              ):
                  continue

              print()
              print("=" * 70)
              print(
                  f"Migrating: {app_name}"
              )
              print("=" * 70)

              try:

                  with open(
                      compose_path,
                      "r",
                      encoding="utf-8"
                  ) as f:

                      data = yaml.safe_load(f)

              except Exception as e:

                  errors.append(
                      f"{app_name}: YAML error: {e}"
                  )

                  continue

              if not isinstance(
                  data,
                  dict
              ):

                  errors.append(
                      f"{app_name}: invalid YAML"
                  )

                  continue

              services = data.get(
                  "services",
                  {}
              )

              if not isinstance(
                  services,
                  dict
              ) or not services:

                  errors.append(
                      f"{app_name}: no services"
                  )

                  continue

              # ==================================================
              # TOP LEVEL NAME
              # ==================================================

              if not data.get("name"):

                  data["name"] = re.sub(
                      r"[^a-zA-Z0-9_-]+",
                      "-",
                      app_name
                  ).strip("-").lower()

              # ==================================================
              # TOP LEVEL X-CASAOS
              # ==================================================

              xc = data.get(
                  "x-casaos"
              )

              if not isinstance(
                  xc,
                  dict
              ):

                  xc = {}

              # ==================================================
              # ID
              # ==================================================

              current_id = xc.get("id")

              if (
                  not isinstance(
                      current_id,
                      str
                  )
                  or not current_id.strip()
              ):

                  xc["id"] = make_id(
                      app_name
                  )

              # IMPORTANT:
              # ID MUST BE DIRECTLY UNDER X-CASAOS
              assert "id" in xc

              # ==================================================
              # MAIN
              # ==================================================

              if not xc.get("main"):

                  if len(services) == 1:

                      xc["main"] = next(
                          iter(services)
                      )

              if not xc.get("main"):

                  errors.append(
                      f"{app_name}: "
                      "cannot determine main service"
                  )

                  continue

              # ==================================================
              # INDEX
              # ==================================================

              if not xc.get("index"):

                  xc["index"] = "/"

              # ==================================================
              # PORT MAP
              # ==================================================

              port = get_existing_port(
                  xc
              )

              if port is None:

                  port = get_compose_port(
                      data
                  )

              if port is None:

                  port = get_environment_port(
                      data
                  )

              # CastSponsorSkip uses host network
              # and exposes HTTP metrics through 9790.
              if (
                  port is None
                  and app_name.lower()
                  == "castsponsorskip"
              ):

                  port = "9790"

              if port is None:

                  errors.append(
                      f"{app_name}: "
                      "cannot determine port_map"
                  )

                  continue

              xc["port_map"] = str(
                  port
              )

              # ==================================================
              # SCHEME
              # ==================================================

              if not xc.get("scheme"):

                  xc["scheme"] = "http"

              # ==================================================
              # ICON
              # ==================================================

              if not xc.get("icon"):

                  icon_files = [
                      "icon.svg",
                      "icon.png",
                      "icon.jpg",
                      "icon.jpeg",
                  ]

                  found_icon = None

                  for icon in icon_files:

                      icon_path = os.path.join(
                          app_dir,
                          icon
                      )

                      if os.path.isfile(
                          icon_path
                      ):

                          found_icon = icon

                          break

                  if found_icon:

                      xc["icon"] = (
                          "https://cdn.jsdelivr.net/gh/"
                          "yassinyl/casa7014@main/"
                          "Apps/"
                          f"{app_name}/"
                          f"{found_icon}"
                      )

                  else:

                      errors.append(
                          f"{app_name}: "
                          "missing icon"
                      )

                      continue

              # ==================================================
              # NORMALIZE LOCALES
              # ==================================================

              xc = normalize_locales(
                  xc
              )

              # ==================================================
              # TITLE
              # ==================================================

              if not xc.get("title"):

                  xc["title"] = {
                      "en_US": app_name
                  }

              # ==================================================
              # TAGLINE
              # ==================================================

              if not xc.get("tagline"):

                  xc["tagline"] = {
                      "en_US": app_name
                  }

              # ==================================================
              # DESCRIPTION
              # ==================================================

              if not xc.get("description"):

                  xc["description"] = {
                      "en_US": app_name
                  }

              # ==================================================
              # CATEGORY
              # ==================================================

              category = xc.get(
                  "category"
              )

              if category not in ALLOWED_CATEGORIES:

                  xc["category"] = CATEGORY_MAP.get(
                      category,
                      "Others"
                  )

              # ==================================================
              # ARCHITECTURES
              # ==================================================

              if not xc.get(
                  "architectures"
              ):

                  xc["architectures"] = [
                      "amd64",
                      "arm64"
                  ]

              # ==================================================
              # VERSION
              # ==================================================

              if not xc.get("version"):

                  xc["version"] = "1.0.0"

              # ==================================================
              # SAVE TOP LEVEL X-CASAOS
              # ==================================================

              data["x-casaos"] = xc

              # ==================================================
              # WRITE FILE
              # ==================================================

              with open(
                  compose_path,
                  "w",
                  encoding="utf-8"
              ) as f:

                  yaml.safe_dump(
                      data,
                      f,
                      sort_keys=False,
                      allow_unicode=True
                  )

              print(
                  f"ID:       {xc['id']}"
              )

              print(
                  f"MAIN:     {xc['main']}"
              )

              print(
                  f"PORT:     {xc['port_map']}"
              )

              print(
                  f"CATEGORY: {xc['category']}"
              )

              print(
                  f"VERSION:  {xc['version']}"
              )

              print(
                  "Migration: OK"
              )

          # ======================================================
          # STOP IF ANY APP FAILED
          # ======================================================

          if errors:

              print()
              print("=" * 70)
              print("MIGRATION ERRORS")
              print("=" * 70)

              for error in errors:

                  print(
                      f"ERROR: {error}"
                  )

              raise SystemExit(1)

          print()
          print("=" * 70)
          print(
              "ALL APPS MIGRATED SUCCESSFULLY"
          )
          print("=" * 70)
          PY

      # ==========================================================
      # SUPPORTED LANGUAGES
      # ==========================================================

      - name: Create supported-languages.json
        run: |
          cat > supported-languages.json <<'EOF'
          [
            "en_US"
          ]
          EOF

      # ==========================================================
      # FIX STORE CONFIG FOR V2
      # ==========================================================

      - name: Normalize store-config.json
        run: |
          set -euo pipefail

          python3 <<'PY'
          import json
          import os

          path = "store-config.json"

          if os.path.isfile(path):

              with open(
                  path,
                  "r",
                  encoding="utf-8"
              ) as f:

                  config = json.load(f)

          else:

              config = {}

          # ------------------------------------------------------
          # Protocol version
          # ------------------------------------------------------

          config["version"] = 2

          # ------------------------------------------------------
          # Stable store ID
          # ------------------------------------------------------

          store_id = config.get(
              "store_id"
          )

          if not isinstance(
              store_id,
              str
          ) or not store_id.strip():

              config["store_id"] = "casa7014-yl"

          # ------------------------------------------------------
          # Name
          # ------------------------------------------------------

          name = config.get(
              "name"
          )

          if isinstance(
              name,
              str
          ):

              config["name"] = {
                  "en_US": name
              }

          elif not isinstance(
              name,
              dict
          ):

              config["name"] = {
                  "en_US": "Casa7014 AppStore"
              }

          elif not config["name"].get(
              "en_US"
          ):

              first = next(
                  iter(
                      config["name"].values()
                  ),
                  "Casa7014 AppStore"
              )

              config["name"]["en_US"] = str(
                  first
              )

          # ------------------------------------------------------
          # Description
          # ------------------------------------------------------

          description = config.get(
              "description"
          )

          if isinstance(
              description,
              str
          ):

              config["description"] = {
                  "en_US": description
              }

          elif description is None:

              config["description"] = {
                  "en_US": "Casa7014 App Store"
              }

          elif isinstance(
              description,
              dict
          ):

              if not description.get(
                  "en_US"
              ):

                  first = next(
                      iter(
                          description.values()
                      ),
                      "Casa7014 App Store"
                  )

                  description["en_US"] = str(
                      first
                  )

          # ------------------------------------------------------
          # Maintainer
          # ------------------------------------------------------

          if not config.get(
              "maintainer"
          ):

              config["maintainer"] = "yassinyl"

          # ------------------------------------------------------
          # Repository URL
          # ------------------------------------------------------

          if not config.get(
              "url"
          ):

              config["url"] = (
                  "https://github.com/"
                  "yassinyl/casa7014"
              )

          # ------------------------------------------------------
          # Save
          # ------------------------------------------------------

          with open(
              path,
              "w",
              encoding="utf-8"
          ) as f:

              json.dump(
                  config,
                  f,
                  indent=2,
                  ensure_ascii=False
              )

              f.write("\n")

          print(
              json.dumps(
                  config,
                  indent=2,
                  ensure_ascii=False
              )
          )
          PY

      # ==========================================================
      # VALIDATE STORE CONFIG
      # ==========================================================

      - name: Validate store-config.json
        run: |
          set -euo pipefail

          python3 <<'PY'
          import json
          import re

          with open(
              "store-config.json",
              encoding="utf-8"
          ) as f:

              config = json.load(f)

          assert config.get(
              "version"
          ) == 2

          assert isinstance(
              config.get("store_id"),
              str
          )

          assert re.fullmatch(
              r"[A-Za-z0-9._-]+",
              config["store_id"]
          )

          assert isinstance(
              config.get("name"),
              dict
          )

          assert isinstance(
              config["name"].get("en_US"),
              str
          )

          assert config["name"]["en_US"].strip()

          assert isinstance(
              config.get("maintainer"),
              str
          )

          print(
              "store-config.json: OK"
          )
          PY

      # ==========================================================
      # VALIDATE APPS
      # ==========================================================

      - name: Validate V2 source
        shell: bash
        run: |
          set -euo pipefail

          python3 <<'PY'
          import os
          import re
          import yaml

          REQUIRED = [
              "id",
              "main",
              "index",
              "port_map",
              "icon",
              "title",
              "category",
              "version",
          ]

          CATEGORIES = {
              "Media",
              "Productivity",
              "Home",
              "Networking",
              "AI",
              "Finance",
              "Social",
              "Developer",
              "Others",
          }

          errors = []

          for app_name in sorted(
              os.listdir("Apps")
          ):

              compose = os.path.join(
                  "Apps",
                  app_name,
                  "docker-compose.yml"
              )

              if not os.path.isfile(
                  compose
              ):
                  continue

              print(
                  f"Checking: {compose}"
              )

              try:

                  with open(
                      compose,
                      encoding="utf-8"
                  ) as f:

                      data = yaml.safe_load(f)

              except Exception as e:

                  errors.append(
                      f"{app_name}: {e}"
                  )

                  continue

              if not isinstance(
                  data,
                  dict
              ):

                  errors.append(
                      f"{app_name}: invalid YAML"
                  )

                  continue

              # --------------------------------------------------
              # Top-level x-casaos
              # --------------------------------------------------

              xc = data.get(
                  "x-casaos"
              )

              if not isinstance(
                  xc,
                  dict
              ):

                  errors.append(
                      f"{app_name}: "
                      "missing top-level x-casaos"
                  )

                  continue

              # --------------------------------------------------
              # ID must be directly under x-casaos
              # --------------------------------------------------

              app_id = xc.get(
                  "id"
              )

              if not isinstance(
                  app_id,
                  str
              ):

                  errors.append(
                      f"{app_name}: "
                      "x-casaos.id missing"
                  )

              elif not re.fullmatch(
                  r"[A-Za-z0-9._-]+",
                  app_id
              ):

                  errors.append(
                      f"{app_name}: "
                      f"invalid id: {app_id}"
                  )

              elif "." not in app_id:

                  errors.append(
                      f"{app_name}: "
                      f"id must contain '.': {app_id}"
                  )

              # --------------------------------------------------
              # Required fields
              # --------------------------------------------------

              for field in REQUIRED:

                  if not xc.get(field):

                      errors.append(
                          f"{app_name}: "
                          f"x-casaos.{field} missing"
                      )

              # --------------------------------------------------
              # Category
              # --------------------------------------------------

              category = xc.get(
                  "category"
              )

              if category not in CATEGORIES:

                  errors.append(
                      f"{app_name}: "
                      f"invalid category: {category}"
                  )

              # --------------------------------------------------
              # Version
              # --------------------------------------------------

              if not isinstance(
                  xc.get("version"),
                  str
              ):

                  errors.append(
                      f"{app_name}: "
                      "version must be string"
                  )

              # --------------------------------------------------
              # Port
              # --------------------------------------------------

              if not isinstance(
                  xc.get("port_map"),
                  str
              ):

                  errors.append(
                      f"{app_name}: "
                      "port_map must be string"
                  )

              # --------------------------------------------------
              # Main service
              # --------------------------------------------------

              services = data.get(
                  "services",
                  {}
              )

              main = xc.get(
                  "main"
              )

              if (
                  isinstance(
                      services,
                      dict
                  )
                  and main not in services
              ):

                  errors.append(
                      f"{app_name}: "
                      f"main service '{main}' "
                      "does not exist"
                  )

              if not errors:

                  print(
                      f"  ID:       {app_id}"
                  )

                  print(
                      f"  PORT:     {xc.get('port_map')}"
                  )

                  print(
                      f"  CATEGORY: {category}"
                  )

                  print(
                      "  OK"
                  )

          if errors:

              print()
              print("=" * 70)
              print("V2 VALIDATION ERRORS")
              print("=" * 70)

              for error in errors:

                  print(
                      f"ERROR: {error}"
                  )

              raise SystemExit(1)

          print()
          print(
              "ALL APPS PASSED V2 SOURCE VALIDATION"
          )
          PY

      # ==========================================================
      # SHOW STORE CONFIG
      # ==========================================================

      - name: Show final store-config.json
        run: |
          echo "======================================"
          echo "FINAL store-config.json"
          echo "======================================"

          cat store-config.json

      # ==========================================================
      # SHOW ONE MIGRATED APP
      # ==========================================================

      - name: Show migrated App
        run: |
          echo "======================================"
          echo "SAMPLE V2 APP"
          echo "======================================"

          FIRST_APP=$(
            find Apps \
              -mindepth 2 \
              -maxdepth 2 \
              -name docker-compose.yml \
              | sort \
              | head -n 1
          )

          if [ -n "$FIRST_APP" ]; then
            cat "$FIRST_APP"
          fi

      # ==========================================================
      # COMMIT MIGRATION
      # ==========================================================

      - name: Commit V2 migration
        shell: bash
        run: |
          set -euo pipefail

          git config \
            user.name \
            "github-actions[bot]"

          git config \
            user.email \
            "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            Apps \
            store-config.json \
            supported-languages.json

          if git diff --cached --quiet; then

            echo "No migration changes."

          else

            git commit \
              -m "chore: migrate app store to CasaOS V2 [skip ci]"

            git push \
              origin \
              HEAD:refs/heads/main

          fi

      # ==========================================================
      # BUILD OFFICIAL V2 DIST
      # ==========================================================

      - name: Build CasaOS V2
        id: build_v2
        uses: IceWhaleTech/build-appstore-action@main
        with:
          source: .
          output: dist
          base-url: https://yassinletaief.tn/casa7014
          report-json: out/build-v2-report.json
          report-title: Casa7014 V2 Build Report

      # ==========================================================
      # VERIFY GENERATED V2 OUTPUT
      # ==========================================================

      - name: Verify V2 output
        shell: bash
        run: |
          set -euo pipefail

          echo "======================================"
          echo "V2 OUTPUT"
          echo "======================================"

          find dist \
            -maxdepth 4 \
            -type f \
            | sort

          echo
          echo "Checking required files..."

          test -f dist/store.json
          test -f dist/index.json

          echo
          echo "store.json:"
          cat dist/store.json

          echo
          echo "index.json:"
          cat dist/index.json

          echo
          echo "V2 BUILD OUTPUT: OK"

      # ==========================================================
      # UPLOAD V2 DIST
      # ==========================================================

      - name: Upload V2 Store
        uses: actions/upload-artifact@v6
        with:
          name: casa7014-v2-store
          path: dist
          if-no-files-found: error

      # ==========================================================
      # UPLOAD REPORT
      # ==========================================================

      - name: Upload V2 build report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: casa7014-v2-report
          path: out/build-v2-report.json
          if-no-files-found: warn
